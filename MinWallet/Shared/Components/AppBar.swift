import SwiftUI

struct AppBar: View {
    var body : some View {
        Text("Hello World")
    }
}

struct AppBar_Previews: PreviewProvider {
    static var previews: some View {
        VStack {
            AppBar()
        }
    }
}
