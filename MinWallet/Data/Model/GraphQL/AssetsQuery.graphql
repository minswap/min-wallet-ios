query AssetsQuery($input: AssetsInput) {
  assets(input: $input) {
    assets {
      currencySymbol
      metadata {
        decimals
        isVerified
        name
        ticker
      }
      tokenName
    }
    searchAfter
  }
}
