namespace mwrust {
  record MinWallet {
    string name;
    string address;
    string reward_address;
    uint8 network_id;
    string encrypted_key;
    uint32 account_index;
  }

  interface WalletStaticMethods {
    MinWallet create_wallet(
      string name,
      string address,
      string reward_address,
      uint8 network_id,
      string encrypted_key,
      uint32 account_index
    );
  }
};
